# Project docs folder (`project-docs/`)

## What changed

- **Convention:** Development journals (architecture, design, new features) live under **`project-docs/`** at the repo root.
- **Migration:** Existing markdown notes were moved from `docs/tavernos/` into `project-docs/`; the old `docs/tavernos/` path was removed. `README.md` here describes how to use the folder.

## What to put here

For each meaningful change, add a new `.md` file that records:

- Steps taken
- CSS or styling changes worth noting
- How dependencies or tooling were installed
- How the feature was wired into the app

## CSS / install

- No new packages; folder-only layout change.
